## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues for `ssannnn/legalfit`. See `docs/agents/issue-tracker.md`.

### Triage labels

This repo uses the default triage labels: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

This repo uses a single-context domain layout: root `CONTEXT.md` plus `docs/adr/`. See `docs/agents/domain.md`.
